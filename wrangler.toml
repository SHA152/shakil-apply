name = "shakil-apply"
main = "worker.js"
compatibility_date = "2024-01-01"

# API proxy lives at api.shakil.fun
# The HTML app lives at apply.shakil.fun (Cloudflare Pages)
routes = [
  { pattern = "api.shakil.fun", custom_domain = true }
]

# Secret: ANTHROPIC_API_KEY — set via Cloudflare dashboard or:
#   npx wrangler secret put ANTHROPIC_API_KEY
