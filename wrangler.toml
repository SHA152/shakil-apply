name = "shakil-apply"
main = "worker.js"
compatibility_date = "2024-01-01"

routes = [
  { pattern = "apply.shakil.fun", custom_domain = true }
]

# Secrets are NOT stored here — set via Cloudflare dashboard or:
#   npx wrangler secret put ANTHROPIC_API_KEY
